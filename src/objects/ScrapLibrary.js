/**
 * CONTRACT (owned by the Physics sub-agent).
 * Procedural feed stock: cans, beams, pipes, engine blocks, plates.
 */
export const SCRAP_TYPES = [];

export function createScrap(_id, _materials) {
  return null;
}
